/**
 * Journal prompt — context / decision / outcome / next 작성 규칙
 */

export const JOURNAL_FORBIDDEN_PHRASES = `## journal 필드 — 절대 사용하지 말 것

다음 표현은 **journal 필드에서만** 쓰지 마세요.

- "기록되지 않음"
- "입력에 명시되지 않음"
- "판단 근거를 알 수 없습니다"

정보가 부족할 때는 **짧고 자연스러운 회고 문장**으로 마무리하세요.`;

export const JOURNAL_PROMPT = `## journal 필드 작성 가이드

각 필드는 **입력 사실만** 바탕으로 하되, **서로 다른 관점**에서 정리합니다.
입력 문장을 필드마다 그대로 복사하지 마세요.

### context — 왜 시작했는가 (배경·동기)

- 작업을 시작하게 된 **배경·상황·동기**를 씁니다.
- [작업 목적] 섹션이 있으면 반영합니다.
- 작업 행위에서 **자연스럽게 유추 가능한 배경**은 풀어 써도 됩니다.
  - 예: "Prompt를 파일로 분리했다" → "프롬프트 코드가 한곳에 모여 있어 역할별로 나누는 작업을 시작했다"
- 입력에 없는 **효과·성과·미래 계획**은 쓰지 않습니다.

### decision — 무엇을 선택했는가 (방향·이유)

- 입력에 **명시된 선택·방향·이유**만 작성합니다.
- "왜 이 방식을 택했는지"가 입력에 드러날 때만 이유를 씁니다.
- 입력에 선택/이유가 없으면, **입력에서 확인 가능한 작업 방향**만 짧게 정리합니다.
- 입력에 없는 **대안 비교·추론된 의도·일반론**은 쓰지 않습니다.

### outcome — 무엇을 완료했는가 (완료 사실)

- **실제로 완료한 작업**만 작성합니다. "무엇을 했는지"에 집중합니다.
- **금지**: 입력에 없는 효과·성과 표현
  - "유지보수성이 향상되었다", "사용자 경험이 개선되었다", "품질이 높아졌다" 등
- **허용**: 완료한 작업을 다른 표현으로 정리
  - "Prompt를 shared, commit, journal 파일로 분리했다" → "프롬프트를 역할별 세 파일로 나누어 각 수정 위치를 분리했다"

### next — 이후 무엇을 할 것인가 (후속 계획)

- [다음 계획] 섹션이 있거나, 입력에 **후속 작업이 명시**된 경우에만 작성합니다.
- 입력에 다음 계획이 없으면 **이번 작업 마무리** 수준으로 짧게 끝냅니다.
- 입력에 없는 **미래 계획·로드맵**을 만들지 않습니다.

### 나쁜 예 vs 좋은 예

**입력:** \`Prompt를 shared, commit, journal 파일로 분리했다.\`

| 필드 | 나쁜 예 (복붙·창작) | 좋은 예 (정리) |
|------|-------------------|---------------|
| context | Prompt를 shared, commit, journal 파일로 분리했다. | 프롬프트 관련 코드가 하나의 파일에 모여 있어, 역할별로 나누는 작업을 진행했다. |
| decision | Prompt를 shared, commit, journal 파일로 분리했다. | shared, commit, journal 역할별로 파일을 나누기로 했다. |
| outcome | 유지보수성이 향상되었다. | Prompt를 shared, commit, journal 세 파일로 나누어 역할별 수정 위치를 분리했다. |`;
